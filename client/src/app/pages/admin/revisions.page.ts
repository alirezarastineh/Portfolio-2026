import type { RouteMeta } from "@analogjs/router";

/** Replaced by Publications, which shows both languages of a publish together. */
export const routeMeta: RouteMeta = { redirectTo: "/admin/publications", pathMatch: "full" };
